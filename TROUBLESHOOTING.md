# 故障排查与降级策略

> KuroTutor 的容错设计原则：错误不崩溃、可重试、有降级、行为可预测。
> 本文档把代码里的重试与降级策略显式化，便于部署者排查问题。

## 1. 模型 API 调用（文本生成）

| 情况 | 行为 |
|---|---|
| 连接失败 / 读取超时 | 自动重试 2 次（指数退避），仍失败则回复「老师暂时联系不上，请稍后再试」 |
| HTTP 429 / 5xx | 同上重试逻辑 |
| HTTP 401 / 4xx | 不重试，记录日志并提示检查 `models.llm` 配置 |
| 长回复超时 | 文本生成走流式接收（首 token 秒回），基本消除长生成超时 |

排查：`docker compose logs kuro | grep "llm failed"`。

## 2. 视觉模型（拍照解题/批改）

| 情况 | 行为 |
|---|---|
| 未配置 `models.vision` | 自动回落主模型（主模型需支持图片输入） |
| 调用失败 | 重试 2 次后降级：回复学生「老师这边看不清图」并建议重拍 |

## 3. 网络搜索 / 题库

| 情况 | 行为 |
|---|---|
| Tavily 失败/无 key | 自动沿 Bing → DuckDuckGo 兜底 |
| jszkk 题库失败 | 跳过，继续下一级题源 |
| 火花题库失败 | 记录日志并跳过（不阻塞出题） |
| 全部题源失败 | 回退 AI 智能生成，并告知学生「网络真题暂时没找到，我直接出几道」 |

## 4. QQ 渠道

| 情况 | 行为 |
|---|---|
| WebSocket 断线 | botpy SDK 自动重连 |
| 容器重启 | `restart: unless-stopped` 自动拉起，排课/复习任务从数据库恢复 |
| Markdown 发送失败 | 自动回退纯文本 |
| 主动推送失败（用户关闭「允许主动发送」或超限） | 记录日志，不重试轰炸；被动回复不受影响 |
| 文件发送失败 | 降级为文本提示（「文件生成好了但暂时发不了」） |

## 5. 文档 / 文件处理

| 情况 | 行为 |
|---|---|
| docx 生成失败 | 保留 Markdown 版讲义，不阻塞备课 |
| Word/PPT 转 PDF 失败（缺 LibreOffice） | 返回带修复建议的错误（安装 libreoffice 或改用 PDF 输入） |
| 图片倾斜/不完整 | 自动透视矫正 + 倾斜矫正（失败自动用原图） |

## 6. 数据层

| 情况 | 行为 |
|---|---|
| SQLite 写锁冲突 | 四段式消息入口把写事务压缩到最短；偶发冲突自动重试 |
| 数据库新增字段 | 启动时自动迁移补列（`_migrate`），无需手动操作 |
| 磁盘占用增长 | 遗忘机制每日自动清理超期消息（默认 180 天）与已完成任务（90 天） |

## 7. 通用排查命令

```bash
kuro doctor                              # 配置/数据库/模型/渠道逐项诊断
docker compose logs --tail 100 kuro      # 最近日志
docker compose run --rm cli kuro backup  # 先备份再折腾
```
