# 产品规格书 — KuroTutor

> 版本：v1.0
> 日期：2026-08-26
> 状态：定稿，可进入开发


## 1. 产品概述

### 1.1 产品定位

**QQ 私聊里的 24 小时 AI 私人老师**

全科（小学到大学）、从零自研、开源。像真人私教一样答疑、拍照解题、批改作业、记错题、复习、定时上 1v1 小课。

### 1.2 核心理念

1. **私人老师**：引导式讲解（先思路后答案），不代写答案。
2. **Agent‑first**：学生在 QQ 私聊里提出的任何要求，都由 Agent 自主理解和满足。功能不是菜单，是 Agent 的工具。
3. **辅助不替代**：不替代学校教学，是学校学习的贴身辅助。
4. **少打扰**：简单题不弹错题本询问（画像驱动），只在该记的时候问。
5. **长内容不刷屏**：超过 2000 字的内容自动分流（讲义 or 分条发送）。
6. **越用越聪明**：自动提炼技能、自动记忆、定期复盘。
7. **学生视角优先**：所有功能验收以学生体验为准。
8. **命令行优先**：所有管理操作（配置、查看、维护）通过交互式命令行完成，无 WebUI。

### 1.3 定位红线

| 做 | 不做 |
|---|---|
| QQ 私聊里的 AI 私人老师 | 独立课程体系替代学校 |
| 全科辅助学习 | 只做单科 |
| Agent‑first 对话式交互 | 菜单式功能列表 |
| 开源（MIT）、从零自研 | 依赖闭源商业平台 |
| 校本同步（可选能力） | 强制绑定学校进度 |
| 命令行管理 | WebUI 管理界面 |

### 1.4 差异化亮点

- **QQ 原生**：不是 Web 套壳，学生最熟悉的入口
- **Agent 交互**：功能是工具，零学习成本
- **真人私教感**：定时课堂 + 自制讲义 + 1v1 定制授课
- **错题闭环**：采集 → 归类 → 复习 → 掌握，不只存不用
- **画像驱动一切**：错题询问、出题难度、课堂编排都由画像决定
- **知识库主动沉淀**：从每次解题中提炼方法卡片，越用越强
- **自进化**：自动提炼技能、自动写记忆、定期复盘
- **命令行管理**：所有运维操作通过 CLI 完成，轻量无依赖


## 2. 目标用户与场景

### 2.1 目标用户

| 学段 | 画像 | 核心诉求 |
|---|---|---|
| 小学 | 注意力短、喜欢图 | 语气亲切、图文丰富、有奖励 |
| 初中 | 作业多、有考试压力 | 解题快、讲解听得懂、错题能复习 |
| 高中 | 应试冲刺 | 效率、方法论、复习计划 |
| 大学/考研 | 自学/专业课 | 概念深度、体系化讲解、讲义质量 |

### 2.2 典型场景

1. 晚上写作业卡住 → 拍照发 QQ → AI 引导式分步讲解 → 主动问“要记入错题本吗？”
2. 明天考试 → “帮我复习二次函数” → AI 查画像+错题+知识库 → 生成复习讲义 → 出巩固题
3. 交作业 → 拍照/发文件 → “帮我批改” → 批改 → 错题归类 → 询问入错题本
4. 周日 19:00 私教课 → AI 按画像备好讲义 → QQ 消息化授课（讲义+分条+提问）→ 课后作业
5. 语音问“牛顿第二定律怎么理解？” → 语音回答 + 图文小结
6. 超长回复（>2000 字）→ 双模式：写讲义系统讲 or 分条消息仿真人（偏好记忆）
7. 拍一整页作业 → AI 自动切题（多题一图 → 每图一题）→ 逐题讲解
8. 发笔记图 → AI 解析内容 → 智能归入笔记本 → “已放入『函数』笔记本”
9. “我想系统学初二数学” → AI 诊断画像 → 设计课程方案 → 确认后生成课程表
10. 管理员通过命令行查看学生学情、管理配置、维护知识库


## 3. 功能全景

> 所有功能以 **Agent 工具** 形态存在，由 Agent 编排调用。

### 3.1 核心功能

| 功能模块 | 具体功能 | 说明 |
|---|---|---|
| 解题 | 拍照解题 | 视觉识别 → 引导式讲解；按画像决定是否询问入错题本 |
| 答疑 | 语音对话 | ASR → 回答 → TTS 语音回复；长回答走双模式 |
| 批改 | 作业批改 | 图片/文字/文件 → 逐题批改 → 错题询问入错题本 → 画像更新 |
| 记忆 | 错题本 | 采集、按知识点归类、查询、复习状态管理 |
| 追踪 | 学习进度 | 知识点掌握度、学习时长、课堂记录 |
| 追踪 | 校本同步（可选） | 登记学校教材/章节/考试进度，私教安排贴合学校节奏 |
| 知识库 | 自研 RAG | 嵌入+重排；随解题主动沉淀方法卡片 |
| 画像 | 学生实力画像 | 学段+知识点能力模型；驱动错题询问、出题难度、课堂编排 |
| 讲义 | 文档讲义 | 生成可编辑、可打印的结构化讲义文档 |
| 课堂 | 定时课堂（1v1 私教） | 排课、自制讲义、QQ 消息化授课、课后闭环 |
| 笔记 | 笔记本功能 | 发笔记图 → 解析 → 智能归类 → 确认入库；支持查询 |
| 搜索 | 网络工具 | 网页抓取 + 网络搜索；搜索供应商可配置 |
| 切题 | 自动切题 | 多题一图 → 每图一题（版面分析 + 锚点检测 + 程序切割） |
| 管理 | CLI 管理 | 交互式命令行完成配置、学情查看、知识库维护、任务管理 |

### 3.2 产品经理补充的关键功能

| 功能 | 说明 | 优先级 |
|---|---|---|
| 错题复习引擎 | 间隔重复调度，到期主动推送复习题 | ★★★ |
| 个性化出题 | 按画像薄弱点生成变式题/巩固题 | ★★★ |
| 入学诊断测试 | 首次使用做水平测评，画像不空转起步 | ★★★ |
| 学习周报 | 掌握度变化、错题统计、复习建议，导出文档 | ★★★ |
| 课程表管理 | 排课日历、上课提醒、请假/补课 | ★★★ |
| 讲义生成流水线 | LLM 写讲义 → 转档 → 课堂使用 | ★★★ |
| 课后作业闭环 | 下课布置 → 下次课前批改 → 画像更新 | ★★ |
| 目标管理 | 学生定目标 → 系统拆解计划并追踪 | ★★ |
| 打卡激励 | 轻游戏化，QQ 主动提醒 | ★ |
| 代码执行沙箱 | 编程题可运行验证 | ★★ |
| 数据导出 | 错题本导出打印复习 | ★★ |
| CLI 管理 | 交互式命令行查看学情、配置、日志、维护 | ★★★ |


## 4. 核心机制设计

### 4.1 学生实力画像（驱动一切）

- **表示**：学段 + 学科→章节→知识点，每点带：掌握度(0-1)、置信度、最近练习时间、错误类型分布
- **更新**：难度加权更新，每次解题/批改后异步更新
- **驱动**：
  1. **错题询问策略**：简单题（已熟练）→ 不问默认不存；中难题+薄弱 → 主动问；同类连续错 → 直接存+推加练
  2. 出题难度、课堂编排、复习调度、长内容模式默认

### 4.2 错题本闭环

```
拍照/批改 → 错题判定 → 归类知识点 → 询问策略 → 入库
  → 间隔重复调度 → 到期主动推送复习 → 复测
    → 掌握归档 / 未掌握强化
```

### 4.3 知识库（自研 RAG，主动沉淀）

- **双库结构**：语料库（讲义/教材/百科，检索用）+ 方法库（题型→方法→步骤→易错点卡片，沉淀用）
- **沉淀机制**：每次解题后提炼方法卡片 → 语义去重合并 → 入库；越用越强

### 4.4 长内容双模式

- **触发**：回复 >2000 字
- **模式① 写讲义系统讲**：生成结构化讲义文档，可保存打印
- **模式② 分条消息发送**：≤500 字/条，逐条发送，可穿插语音，仿真人节奏
- **选择**：首次触发给选项 → 记入偏好 → 之后默认执行；随时可改

### 4.5 对话编排（无感体验）

- **优先级队列**：P0 学生新消息（最高）→ P1 后续 → P2 推送（不抢对话）→ P3 后台
- **语境感知打断**：补充→并入；修正→重定向；换题→开新轮；打断缓存可恢复
- **无感分段**：相关性判断 + 时间维度 → 自动开新段；学生无感知
- **上下文分层压缩**：最近若干轮原文 → 中间轮摘要 → 更早剪掉（记忆体系接管）

### 4.6 定时课堂与课程体系（统一调度）

- **所有定时任务本质上都是统一调度**：备课/提醒/开课/下课/作业提醒/复习推送/周报
- **两类课**：
  - **单堂课**：学生提需求 → 协商时间 → 建单次课
  - **系列课（系统教学）**：学生提目标 → Agent 读画像设计课程方案（按接受度排课）→ 确认 → 建课程+课表
- **课前备课**：读画像+大纲+知识库 → 生成讲义 → 互动题+作业 → 状态就绪
- **课后闭环**：课堂总结 → 作业布置 → 画像更新 → 错题沉淀 → 知识库沉淀 → 进度记录 → 系列课动态调整 → 触发下次备课

### 4.7 自进化机制（越用越聪明）

- **自动技能生成**：每次交互后提炼有效做法 → 生成技能定义 → 放自动目录（不覆盖手工），可禁用/删除
- **自动写入记忆**：异步提取学生事实（目标/考试/偏好/纠正）→ 写入长期记忆；学生纠正优先
- **定期复盘**：定期反思效果 → 优化核心技能

### 4.8 自动切题

- **方案**：版面分析 + 锚点检测 + 程序切割
- **小题处理**：大题带小题默认整体保留（共用题干、有依赖）
- **兜底**：切不出 → 整图走解题（不阻塞）

### 4.9 笔记本功能

- 发笔记图 → 异步解析（文字识别+视觉理解）→ 智能归类（语义匹配三档）→ 确认入库
- 查询：按笔记本/关键词检索
- 复习联动：复习推送时引用相关笔记

### 4.10 CLI 管理功能（替代 WebUI）

所有管理操作通过交互式命令行完成：

| 管理功能 | 说明 |
|---|---|
| 配置管理 | 初始化、查看、修改、校验配置 |
| 学生管理 | 查看学生列表、详情、数据删除（合规） |
| 学情查看 | 画像概览、错题列表、进度日志 |
| 知识库管理 | 统计、重建索引、导入语料 |
| 任务管理 | 查看定时任务、执行状态 |
| 健康检查 | 配置/依赖/数据库/渠道连接状态 |
| 日志查看 | 实时日志、历史日志检索 |
| 数据导出 | 错题本导出、学习报告导出 |


## 5. 非功能性需求

| 维度 | 要求 |
|---|---|
| 性能 | QQ 消息响应 < 10s；课堂消息近实时 |
| 成本 | BYOK（自带模型 key）；成本可观测 |
| 部署 | Docker Compose 一键起，普通服务器可跑，无 GPU 依赖 |
| 稳定性 | 错误不崩溃、可重试、有日志、行为可预测 |
| 安全 | Agent 沙箱限定工作区，禁系统设置，API 端点白名单 |
| 可靠性 | 数据定时备份；API 故障降级 |
| 可测试 | 内置评测集，版本迭代防退化 |
| 隐私 | 学生数据本地存储、最小收集、提供导出/删除 |
| 管理 | 命令行交互式管理，无需额外 Web 服务端口 |


## 6. 验收标准

### 6.1 技术验收

- 所有模块单元测试通过
- 代码通过静态检查
- Docker Compose 可一键启动
- 配置校验通过
- 所有 CLI 命令可执行

### 6.2 学生视角验收（强制）

**必须代入 4 个学段人物角色**：小学 / 初中 / 高中 / 大学

**6 个通用问题**（每个功能必问）：
1. 我想让老师做事，几步？顺不顺手？
2. 老师回应像不像真老师？
3. 讲解答不答得懂？有没有先引导我自己想？
4. 会不会烦我？（询问时机、推送频率）
5. 我要的东西拿不拿得到？手机上能不能直接用？
6. 老师记不记得我？（连续对话、水平、偏好）

**底线**：学生体验明显不适 → 打回修复，不准验收通过。


## 7. 里程碑规划

### M1 —— 跑通“Agent 私人老师”主链路

- QQ 渠道接入、Agent 骨架、对话编排
- 拍照解题、画像、错题本
- 知识库、讲义生成、语音工具
- 笔记本功能、自动切题、作业批改、复习引擎
- CLI 管理命令（配置/学生/学情/健康检查）

### M2 —— 闭环与课堂

- 作业批改完善、复习引擎完整
- 个性化出题（本地题库 + 生成 + 绘图）
- 定时课堂完整（排课+备课+授课+课后闭环+提醒+应急）
- 学习周报、校本同步
- 自进化完整
- CLI 命令扩展（知识库管理/任务管理/数据导出）

### M3 —— 产品化与开源准备

- 沉浸式课堂增强
- 入学诊断/目标管理/打卡
- 公式图表渲染/代码沙箱
- 数据导出/评测集/CLI 完善
- 开源发布准备（全部做完再公开）


## 8. 开源策略

| 项目 | 决策 |
|---|---|
| 名称 | KuroTutor |
| 许可证 | MIT |
| 发布节奏 | **全部做完再公开**（开发全程私有，代码/文档整理干净后一次性公开）|
| 部署方式 | Docker 一键部署，BYOK（自带模型 key）|
| 管理方式 | 命令行交互式管理 |
| 文档要求 | 中文 README + 文档站 + 评测集 + 隐私声明 |
| 社区扩展 | 技能与插件开放扩展 |
