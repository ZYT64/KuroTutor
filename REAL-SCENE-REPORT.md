# 真实场景测试报告

> 项目 CLAUDE.md §5.4 强制要求：只跑单测不算完成，必须完成至少一次真实场景端到端测试并记录。
> 本项目录（M1 基石阶段）。

---

## 报告 1 · 控制台渠道端到端会话（学生全流程）

【任务编号】M1-foundation-console-e2e
【测试场景】在本地用控制台渠道扮演学生，与 KuroTutor 老师对话；验证「学生建档 → 消息处理 → 老师回复 → 会话/消息落库」完整链路。
【测试时间】2026-08-26
【测试输入】
```
kuro serve --channel console --student demo1
> 你好老师
> 帮我看看二次函数求根
> /quit
```
【预期输出】每次学生消息后收到老师回复；学生首次出现自动建档；消息写入数据库。
【实际输出】
```
KuroTutor 控制台渠道已启动。
[KuroTutor] [echo] 已收到：你好老师
[KuroTutor] [echo] 已收到：帮我看看二次函数求根
控制台渠道已关闭。
```
学生 `demo1` 已建档，`student list` / `student show` 可见，消息入 `session/message` 表。
【判断】通过
【问题记录】⚠️ 当前用 echo 离线模型，仅验证链路，不产出真实教学讲解。真实讲解需接入在线 LLM（配置 `models.llm`）。

---

## 报告 2 · Agent 工具调用闭环（含真实 DB 写入）

【任务编号】M1-foundation-tools-e2e
【测试场景】验证「错题本 / 知识库方法卡片 / 笔记本」工具是真实存储读写，非空壳：写入 → 落库 → 查询回读。
【测试输入】通过 Agent 主循环触发真实工具（`kb_deposit`、`wrongbook_add`、`notebook_add` 等），随后用对应查询工具回读。
【预期输出】写入返回成功提示；查询能检索到写入内容（含知识点名称而非数字 ID）。
【实际输出】（见 `tests/test_tools.py`，全部通过）
- `kb_deposit` → `kb_search` 命中「二次函数求根」
- `wrongbook_add` → `wrongbook_query` 显示「（二次函数）」且状态「待复习」
- `notebook_add` → `notebook_query` 命中主题
【判断】通过
【问题记录】无。已修复「查询只显示知识点 ID 不显示名称」的体验粗糙点。

---

## 报告 3 · CLI 管理命令冒烟

【任务编号】M1-foundation-cli-smoke
【测试场景】逐条执行 CLI 管理命令，确认可用且输出正确。
【测试输入】`kuro version / config show / config validate / config get / config set / agent tools / student list / student show / kb status / doctor / serve --channel console`
【预期输出】每条命令返回结构化、美观的中文输出；配置校验给出可操作建议；doctor 逐项诊断。
【实际输出】全部可用，退出码合理；`config set` 校验后写盘；`doctor` 显示配置/数据库/模型/渠道状态表；`student remove` 带二次确认（合规）。
【判断】通过
【问题记录】`kuro agent skills` 如实上报「技能系统规划于 M2，当前 0 个」，不伪装数据。

---

## 报告 4 · 拍照解题（真实 DeepSeek 文本 + 视觉模型）

【任务编号】M1-vision-solve-photo-e2e
【测试场景】发一张数学题图片（`x²-5x+6=0`），Agent 自主调用 `solve_photo`（视觉模型读题），再打磨成引导式讲解。
【测试时间】2026-08-26
【测试输入】图片 `data/workspaces/incoming/test_math.png`（Pillow 生成的题目图），文本 `deepseek-chat` + 视觉 `deepseek-v4-flash-vision-exp`。
【预期输出】
- 视觉读题正确（转写题目）；
- Agent 不直接甩答案，先给思路、向学生提问、给提示，最后邀请学生先想（引导式、学段语气）；
- 不崩溃、结果落库。
【实际输出】
- 视觉模型读出「一元二次方程 x²-5x+6=0」并给出思路/步骤/答案(x=2,3)/方法卡片；
- 文本 Agent 输出（节选）：*「小明，这道题我帮你解出来了，不过我不直接告诉你答案，先陪你理清思路……先给你思路提示：因式分解法/求根公式法……哪两个数相乘等于 6、相加又等于 -5 呢？你先想想，想到了告诉我」*
【判断】通过
【问题记录】无。视觉/文本分工清晰：视觉负责准确读题+结构化方法，文本负责引导式教学打磨，二者可各自换厂商。

---

## 报告 5 · 真实文本模型对话（控制台渠道）

【任务编号】M1-llm-conversation-e2e
【测试场景】控制台渠道，真实 LLM（`deepseek-chat`）回答学生提问。
【测试输入】`kuro serve --channel console --student real1`，输入「老师，帮我讲讲勾股定理」。
【预期输出】老师以昵称称呼、引导式、不甩结论。
【实际输出】*「好的real1！勾股定理是个超级重要的老朋友……先问你个小问题：勾股定理说的是哪三条边之间的关系？你先回忆一下……你先说说看，我再帮你理清楚~」*
【判断】通过
【问题记录】无。

---

## 报告 6 · 错题本 / 知识库沉淀闭环（真实 API，确定性）

【任务编号】M1-closed-loop-e2e
【测试场景】拍照解题后，验证「读题判分 → 画像更新 → 方法卡沉淀 → 错题记录/询问」是否**确定性发生**（不依赖模型心情）。分两路径：
- **RECORD 路径**：预置薄弱知识点，学生作答 `x=2`（错，正解 `x=2,3`）。
- **ASK 路径**：全新学生首次做错，走真实控制台渠道。
【测试输入】真实 `deepseek-v4-flash-vision-exp` + `deepseek-chat`，题目图 `x²-5x+6=0`。
【预期输出】RECORD 路径：错题本 +1、方法卡 +1、掌握度下降；ASK 路径：方法卡 +1、不强写错题本、Agent 询问是否记录。
【实际输出】
- RECORD 路径：`已沉淀方法卡片` + `已记入错题本`；库里 wrong_book=1、method_card=1，掌握度 0.4→0.28。
- ASK 路径（控制台渠道）：库 method_card=1、wrong_book=0；Agent 尾部「那这道题你要不要记进错题本，以后帮你复习？」。
【判断】通过
【问题记录】✅ 已修复：入口在跑 Agent 前未关闭写事务导致 SQLite「database is locked」，改为三段式后正常。

## 报告 7 · 笔记本发图 → 解析 → 智能归类入库（真实 API）

【任务编号】M1-notebook-photo-e2e
【测试场景】发一张「函数笔记」图片，验证视觉解析 + 智能归类 + 入库。
【测试输入】Pillow 生成的笔记图；真实视觉模型。
【预期输出】解析出学科/主题/摘要，归类进「数学」笔记本，入库一条，返回确认。
【实际输出】`已解析并存入笔记本「数学」（#1）——主题：函数`；库中 notebook=「数学」、source=image、摘要含「函数的定义以及一次函数和二次函数的表达式」。
【判断】通过
【问题记录】无。

---

## 总结论

本阶段（M1 基石 + 视觉 + 画像 + 闭环）真实场景测试：**报告 1~7 全部通过**。
核心闭环 —— **拍照解题 → 读题判分 → 画像更新 → 方法卡沉淀 → 错题记录/询问** 已完成并**确定性**落库；
**笔记本发图 → 解析 → 归类 → 入库** 亦真实跑通。不再依赖模型心情或假数据。
**尚未覆盖的场景**（如实标注，待后续落地）：作业批改、OCR/版面/自动切图、讲义、语音、复习引擎、调度器、向量检索、真实 QQ 后端联调、定时课堂、自进化。

> 结论：**本阶段（M1 核心 + 视觉 + 闭环）可验收**。M1 完整目标（含全部模型驱动功能）未全部完成，见 `SUGGESTIONS.md` 待办清单。


---

## 【任务编号】Agent 能力真实场景测试（AGT-E2E-001）
【测试场景】模拟真实学生完整旅程，走真实 Agent 循环（主模型 GLM glm-5.3-flash + 视觉 DeepSeek + 全部领域工具），经 MessageEntry 完整入口（分段/压缩/上下文）驱动，非直调工具。
【测试时间】2026-08-28
【测试输入】7 个场景的真实学生消息（文本 2 条 + 真实题目图 1 张 + 真实整卷物理图 1 张 + 组卷/搜索/课件指令 3 条 + 情绪化「完全没懂」1 条）
【预期输出】各场景由 Agent 自主选择并调用正确工具，产生落库/落盘副作用，回复符合教学人设
【实际输出】
- S1 讲题（文本）：直接引导式讲解（无工具需求，符合预期）——22s
- S2 拍照解题：solve_photo×2（读题+教学两轮）→ 引导式讲解——37s
- S3 题集录入：split_photo×2 → image_understand×11（逐题视觉确认）→ bank_add×4 → 「4 道题收进题集（编号 14~17），含考点表格」——212s
- S4 提取组卷：bank_extract → exports/题集_20260828_*.docx 真实生成——15s
- S5 网络搜索：web_search×2 → web_fetch×2 → 引用真实网页内容讲解两种证明——82s
- S6 文档生成：doc_write → docs/二次函数课件.pptx 真实生成（8 页）——36s
- S7 完全没懂→自动录错题：Agent 自主 bank_add（策略生效）；重复题被去重护栏正确拦截后，Agent 安抚学生并慢速重讲——231s
【判断】通过（7/7）
【问题记录】
1. 首轮测试脚本误用 resp.tool_calls 断言（只含 Agent 最后一轮），已改为包装 registry.execute 记录全部真实调用；
2. S3 全程 212s（含 11 次视觉确认），QQ 场景建议入口已回「正在看」提示，长耗时可接受但后续可优化（如录入批量跳过逐题视觉确认）；
3. S7 中 Agent 对已录入题的回复说「帮你收进题集了」措辞略不精确（实际是早已录入），属小瑕疵不影响体验。
【测试脚本】scripts/agent_scene_test.py（可重复执行；重跑时 S7 走去重拦截分支亦判定通过）


---

## 【任务编号】学生端体验优化（AGT-OPT-001）
【测试场景】S3 题集录入 / S5 网络搜索的耗时优化（真实 Agent 循环 + 真实 GLM）
【测试时间】2026-08-28
【优化内容】
1. split_photo/split_document 输出附带每块切图的 OCR 文字（300 字内）→ Agent 录入 bank_add 直接引用，**免去逐图 image_understand**（11 次视觉调用 → 0 次）；
2. 同一张图重复切题 → 结果缓存（存 WorkingContext，路径+mtime 匹配），重复调用零成本返回；
3. web_search 工具描述明确「结果已含摘要，无需再 web_fetch 原文」→ 减少重复抓取。
【测试输入】同 AGT-E2E-001 的 S3/S5 消息（真实物理卷图 + 搜索指令）
【预期输出】工具链中不再出现 image_understand 链；耗时显著下降
【实际输出】
- S3：212s（image_understand×11）→ 优化后 **135s**（image_understand×0；重复 split 命中缓存 0 成本）
- S5：web_fetch 由 2 次降为 0-1 次，搜索讲解行为正常
【判断】通过
【问题记录】
1. 残余耗时为真实工作量（整页 OCR + 跨页连续性视觉判断 + GLM 多轮生成），属合理区间；
2. 题集去重按题干前 80 字精确匹配，OCR 每次结果的细微差异可能产生近似重复条目——真实学生场景影响小，后续可引入模糊去重（已记 M2 候选）。


---

## 【任务编号】M2 全模块（M2-E2E-001）
【测试场景】定时课堂（排课/备课/闭环/改期/系列课）+ 学习周报 + 校本同步，走真实 Agent 循环（GLM 主模型）
【测试时间】2026-08-29
【测试输入】7 个场景的学生自然语言消息（约课/查课表/提前备课/登记学校进度/要周报/报系列课/改期）
【预期输出】Agent 自主调用课堂/周报/校本工具并产生真实副作用（课程表/讲义/Word 周报）
【实际输出】7/7 通过：
- M2-1 排课：schedule_class → 课程表落库（周日 19:00 数学课）22s
- M2-2 查课表：course_list → 带状态回复 19s
- M2-3 备课：prepare_class → **Agent 自主组合拳**（course_list→prepare_class→lecture_gen→doc_read→plot_function）→ 讲义落盘 + 重点提炼 169s
- M2-4 校本同步：school_sync → 北师大版/全等三角形/期中考登记 23s
- M2-5 周报：weekly_report → LLM 润色（亮点/提醒/建议）+ Word 导出 69s
- M2-6 系列课：schedule_class → LLM 大纲设计 → 3 节课排期落库 174s
- M2-7 应急改期：reschedule_class → 时间迁移 21s
【判断】通过（7/7）
【问题记录】
1. 自进化事实提取真机报 AttributeError('student_id')（Message 无该字段，需经 Session 关联）——已修复并加回归测试；
2. 系列课排课时 Agent 调了 3 次 schedule_class（每节一次），行为正确但有冗余——记 M3 候选优化。
【备注】自进化记忆在真机每条消息触发（6 的倍数），开通后可降低频率。
